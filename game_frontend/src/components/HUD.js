import React from 'react';

// PUBLIC_INTERFACE
/**
 * HUD - Heads-Up Display for score, lives, gems, etc.
 */
const HUD = () => (
  <div className="hud">
    {/* TODO: Implement in-game HUD display */}
    <span>HUD (Stub)</span>
  </div>
);

export default HUD;
