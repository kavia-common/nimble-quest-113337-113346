import React from 'react';

// PUBLIC_INTERFACE
/**
 * SettingsOverlay - Modal for sound, controls, and other preferences.
 */
const SettingsOverlay = () => (
  <div className="overlay settings-overlay">
    {/* TODO: Implement settings form */}
    <h2>Settings (Stub)</h2>
  </div>
);

export default SettingsOverlay;
