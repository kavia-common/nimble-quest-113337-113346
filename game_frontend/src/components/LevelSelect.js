import React from 'react';

// PUBLIC_INTERFACE
/**
 * LevelSelect - UI for choosing which level or world to play
 */
const LevelSelect = () => (
  <div className="level-select">
    {/* TODO: Implement level selection */}
    <h2>Level Select (Stub)</h2>
  </div>
);

export default LevelSelect;
